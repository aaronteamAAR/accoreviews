---
import Layout from '../layouts/Layout.astro';
import BlogCard from '../components/blogs/BlogCard.astro';
import { techBlogs } from '../demoblogs/tech-blog.js';
import { sportBlogs } from '../demoblogs/sport-blogs';
import { fashionBlogs } from '../demoblogs/fashion-blogs';
import { artBlogs } from '../demoblogs/art-blogs';
import BlogPopularComponent from '../components/blogs/BlogPopularComponent.astro';

// interface Post {
//   id: number;
//   title: string;
//   body: string;
// }

// let posts: Post[] = [];

// try {
//   const response = await fetch('https://jsonplaceholder.typicode.com/posts?_limit=5');
//   posts = await response.json();
// } catch (error) {
//   console.error('Failed to fetch posts:', error);
// }
---

<Layout pageTitle="Accoreviews">
	<BlogPopularComponent category="Tech" dataType={techBlogs} column="3" />
	<BlogPopularComponent category="Sports" dataType={sportBlogs} column="3" />
	<BlogPopularComponent category="Fashion" dataType={fashionBlogs} column="2" />
	<BlogPopularComponent category="Arts" dataType={artBlogs} column="3" />
</Layout>
